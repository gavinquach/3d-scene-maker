import { memo } from "react";

const Properties = (props) => {
    return (
        <div className="select-none">
            <h1>Properties</h1>
        </div>
    );
};

export default memo(Properties);
